# 复制本文件为 .env 并填写实际值。
# systemd 服务通过 EnvironmentFile 读取；也可以把密钥填在 config.yaml 的 primary/ 段。

# Server酱（微信推送，无需 VPN）—— 推荐必填
SERVERCHAN_SENDKEY=

# 企业微信群机器人 webhook —— 可选
WECOM_WEBHOOK=

# 以下为可选的一手源密钥（二选一：填这里，或填 config.yaml 的 primary 段）
FRED_API_KEY=
QUIVER_TOKEN=
BARGO_API_KEY=
